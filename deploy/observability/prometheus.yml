global:
  scrape_interval: 5s
  evaluation_interval: 15s

rule_files:
  - /etc/prometheus/alerts.yml

scrape_configs:
  - job_name: synapse
    static_configs:
      - targets: [otel-collector:9464]
        labels:
          environment: production
